

## Rename "Live Practice" Tab to "View Training"

**File: `src/components/SimulationTraining.tsx`**

1. Change the "Live Practice" `TabsTrigger` label from "Live Practice" to "View Training"
2. Keep the `Eye` icon as it fits the "view" concept

Single line change — the tab value (`"live"`) stays the same internally.


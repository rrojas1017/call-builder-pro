

## Change Gym Icon to Dumbbell

A one-line change in `src/components/AppSidebar.tsx`:

- Replace the `FlaskConical` import with `Dumbbell` from `lucide-react`
- Update the nav item for Gym to use `Dumbbell` instead of `FlaskConical`

If `FlaskConical` is not used anywhere else in the file, remove it from the import statement entirely.

